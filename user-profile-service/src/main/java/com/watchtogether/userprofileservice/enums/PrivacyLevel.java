package com.watchtogether.userprofileservice.enums;

public enum PrivacyLevel {
    PUBLIC,    // Профиль виден всем
    PRIVATE,   // Профиль скрыт, виден только друзьям
    HIDDEN     // Профиль скрыт полностью (только для администрации)
}
